/** Shown in the footer. Change the name and add any profile links you want. */
export const siteDeveloper = {
  displayName: "Tharuksha",
  links: [] as ReadonlyArray<{ label: string; href: string }>,
};
